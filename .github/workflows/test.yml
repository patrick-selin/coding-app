name: Debug Echo Workflow

on:
  push:
    branches:
      - main

jobs:
  echo-debug:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Echo a message
        run: echo "Hello, this is a debug message!"
